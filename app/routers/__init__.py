# Routers package.
#
# Intentionally empty for now. Per the project scope for this milestone,
# no CRUD endpoints or route modules are added yet. Task-related routes
# (e.g. app/routers/tasks.py) will be introduced in a later step once
# the SQLite + SQLModel storage layer (see ADR-001) is implemented.
